<!DOCTYPE html>
<html lang="zh-cmn-Hans">
  <head>
    <meta charset="utf-8">
    <title>Slintta</title>
    <meta name="description" content="Slintta's home page">
    <meta name="author" content="Slintta & Asaka">
    <link rel="icon apple-touch-icon" type="image/png" href="/static/image/head.png">
    <link rel="stylesheet" href="/static/typo.css">
    <link rel="stylesheet" href="/static/style.css">
    <!--[if lt IE 9]>
      <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body>
    <div id="container">
      <div id="header">
      </div>

      <% posts.forEach(function(post, index) { %>
        <div class="post">
          <div class="title">
            <h1><a href="<%= "/post/" + post.timestamp.toString(36) %>"><%= post.title %></a></h1>
          </div>
          <div class="date">
            <p><%= post.date %></p>
          </div>
          <div class="content">
            <%- post.content %>
          </div>
        </div>

        <% if (index < posts.length - 1) { %>
          <hr />
        <% } %>

      <% }) %>

      <div id="footer">
        <hr />
        <p>❤ designed by Slintta &amp; made by Asaka</p>
      </div>
    </div>
  </body>
</html>
